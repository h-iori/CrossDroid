<Page
    x:Class="CrossDroid.Windows.Views.DevicesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BgMain}">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition FromHorizontalOffset="0" FromVerticalOffset="28"/>
        </TransitionCollection>
    </Page.Transitions>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Devices Manager" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextStrong}" />
                <TextBlock Text="Manage paired devices and pair new peers" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0" />
            </StackPanel>

            <!-- Tabs/Navigation -->
            <Pivot Grid.Row="1" Background="Transparent">
                
                <!-- Tab 1: Paired Devices -->
                <PivotItem Header="Paired Devices" Margin="0,12,0,0">
                    <Grid RowSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,0,0,8">
                            <Button Click="RefreshPaired_Click" Height="32" Background="{StaticResource BgSurface}" BorderBrush="{StaticResource BorderSubtle}" Foreground="{StaticResource TextBody}" Padding="8,4">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="11"/>
                                    <TextBlock Text="Refresh" FontSize="11"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <!-- Paired Devices List -->
                        <ListView x:Name="PairedDevicesList" Grid.Row="1" SelectionMode="None" IsItemClickEnabled="False">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0,0,0,10" />
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Background="{StaticResource BgElevated}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" Padding="12">
                                        <Grid RowSpacing="8">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            
                                            <!-- Upper: Icon & Name -->
                                            <Grid Grid.Row="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" CornerRadius="16" Background="{StaticResource BgSurface}" Width="32" Height="32" Margin="0,0,12,0" VerticalAlignment="Center">
                                                    <FontIcon Glyph="{Binding IconGlyph}" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource NeonPrimary}" FontSize="14"/>
                                                </Border>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}"/>
                                                        <Border VerticalAlignment="Center" Padding="4,1" CornerRadius="4" Background="{Binding StatusBackground}">
                                                            <TextBlock Text="{Binding Status}" FontSize="9" FontWeight="Bold" Foreground="{Binding StatusForeground}"/>
                                                        </Border>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding Info}" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                            </Grid>
                                            
                                            <!-- Lower: Actions Row -->
                                            <Grid Grid.Row="1" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,1,0,0" Padding="0,8,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <Button Grid.Column="0" Click="BlockDevice_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" Padding="0">
                                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                                        <FontIcon Glyph="&#xE8F8;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{StaticResource AccentCyan}"/>
                                                        <TextBlock Text="Block" FontSize="11" Foreground="{StaticResource AccentCyan}"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Grid.Column="1" Click="RenameDevice_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" Padding="0">
                                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                                        <FontIcon Glyph="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{StaticResource TextBody}"/>
                                                        <TextBlock Text="Rename" FontSize="11" Foreground="{StaticResource TextBody}"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Grid.Column="2" Click="DeleteDevice_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" Padding="0">
                                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                                        <FontIcon Glyph="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{StaticResource ColorError}"/>
                                                        <TextBlock Text="Remove" FontSize="11" Foreground="{StaticResource ColorError}"/>
                                                    </StackPanel>
                                                </Button>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </PivotItem>

                <!-- Tab 2: Discover & Pair -->
                <PivotItem Header="Discover &amp; Pair" Margin="0,12,0,0">
                    <Grid RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" /> <!-- Discovered list -->
                            <RowDefinition Height="Auto" /> <!-- PIN pairing -->
                        </Grid.RowDefinitions>

                        <!-- Discovered Nearby Devices -->
                        <Grid Grid.Row="0" RowSpacing="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="180" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <TextBlock Text="Nearby Devices" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}" VerticalAlignment="Center"/>
                                <ProgressRing x:Name="ScanningProgress" IsActive="True" Width="16" Height="16" HorizontalAlignment="Right" Foreground="{StaticResource NeonPrimary}"/>
                            </Grid>

                            <!-- Nearby devices list -->
                            <ListView x:Name="NearbyDevicesList" Grid.Row="1" SelectionMode="None" IsItemClickEnabled="False">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Margin" Value="0,0,0,8" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="10" Background="{StaticResource BgElevated}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" Padding="12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <!-- Icon -->
                                                <Border Grid.Column="0" CornerRadius="14" Background="{StaticResource BgSurface}" Width="28" Height="28" Margin="0,0,12,0" VerticalAlignment="Center">
                                                    <FontIcon Glyph="{Binding IconGlyph}" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource AccentCyan}" FontSize="12"/>
                                                </Border>

                                                <!-- Details -->
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}"/>
                                                    <TextBlock Text="{Binding Address}" FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0"/>
                                                </StackPanel>

                                                <!-- Pair action -->
                                                <Button Grid.Column="2" Content="Pair" Click="PairDevice_Click" Height="28" Background="{StaticResource BgSurface}" BorderBrush="{StaticResource AccentCyan}" BorderThickness="1" Foreground="{StaticResource AccentCyan}" VerticalAlignment="Center" Padding="8,2" FontSize="11"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>

                        <!-- PIN / Camera Pairing Controls -->
                        <Border Grid.Row="1" CornerRadius="12" Background="{StaticResource BgElevated}" BorderBrush="{StaticResource AccentCyanBorderBrush}" BorderThickness="1.2" Padding="16">
                            <StackPanel Spacing="12">
                                <TextBlock Text="Pair via Verification PIN" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}" />
                                <TextBlock Text="Enter the 4-digit PIN displayed on the other device below." FontSize="11.5" Foreground="{StaticResource TextSecondary}"/>

                                <!-- PIN Text Boxes -->
                                <StackPanel Spacing="6">
                                    <Grid ColumnSpacing="8" HorizontalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="40" />
                                        </Grid.ColumnDefinitions>

                                        <!-- PIN slots (4 Digits) -->
                                        <TextBox Grid.Column="0" x:Name="PinBox1" MaxLength="1" TextAlignment="Center" FontSize="16" FontWeight="Bold" BorderBrush="{StaticResource BorderSubtle}" Background="{StaticResource BgSurface}" Foreground="{StaticResource TextStrong}" TextChanged="PinBox_TextChanged" Width="40" Padding="0,6"/>
                                        <TextBox Grid.Column="1" x:Name="PinBox2" MaxLength="1" TextAlignment="Center" FontSize="16" FontWeight="Bold" BorderBrush="{StaticResource BorderSubtle}" Background="{StaticResource BgSurface}" Foreground="{StaticResource TextStrong}" TextChanged="PinBox_TextChanged" Width="40" Padding="0,6"/>
                                        <TextBox Grid.Column="2" x:Name="PinBox3" MaxLength="1" TextAlignment="Center" FontSize="16" FontWeight="Bold" BorderBrush="{StaticResource BorderSubtle}" Background="{StaticResource BgSurface}" Foreground="{StaticResource TextStrong}" TextChanged="PinBox_TextChanged" Width="40" Padding="0,6"/>
                                        <TextBox Grid.Column="3" x:Name="PinBox4" MaxLength="1" TextAlignment="Center" FontSize="16" FontWeight="Bold" BorderBrush="{StaticResource BorderSubtle}" Background="{StaticResource BgSurface}" Foreground="{StaticResource TextStrong}" TextChanged="PinBox_TextChanged" Width="40" Padding="0,6"/>
                                    </Grid>
                                </StackPanel>

                                <Button Content="Verify PIN &amp; Connect" Click="VerifyPin_Click" HorizontalAlignment="Stretch" Height="36" Background="{StaticResource NeonPrimary}" Foreground="{StaticResource TextStrong}" BorderThickness="0" FontSize="13" FontWeight="SemiBold"/>

                                <MenuFlyoutSeparator Background="{StaticResource BorderSubtle}" Margin="0,4,0,4"/>

                                <!-- Camera Scan -->
                                <TextBlock Text="Pair via QR Code Scan" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}"/>
                                <Button Content="Open Camera Scanner" Click="OpenCamera_Click" Background="{StaticResource BgSurface}" BorderBrush="{StaticResource BorderSubtle}" Foreground="{StaticResource TextBody}" HorizontalAlignment="Stretch" Height="32" FontSize="12"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </PivotItem>
            </Pivot>
        </Grid>
    </ScrollViewer>
</Page>
