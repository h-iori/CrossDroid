<Page
    x:Class="CrossDroid.Windows.Views.DevicesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:CrossDroid.Windows.Views"
    mc:Ignorable="d"
    Background="{StaticResource BgMain}">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition FromHorizontalOffset="0" FromVerticalOffset="28"/>
        </TransitionCollection>
    </Page.Transitions>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20" Spacing="4">
            <TextBlock Text="Saved Devices" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextStrong}" />
            <TextBlock Text="Manage and configure previously trusted devices" FontSize="12" Foreground="{StaticResource TextSecondary}" />
        </StackPanel>

        <!-- Paired Devices List (Enterprise Cyberpunk styling) -->
        <ListView x:Name="PairedDevicesList" Grid.Row="1" SelectionMode="None" IsItemClickEnabled="False">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,12" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:DeviceViewModel">
                    <Border CornerRadius="16" Background="{StaticResource BgElevated}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1.2" Padding="16">
                        <Grid RowSpacing="12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Top Row: Icon, Name and Status -->
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Cyberpunk Round Icon Box -->
                                <Border Grid.Column="0" CornerRadius="18" Background="{StaticResource BgSurface}" Width="36" Height="36" Margin="0,0,16,0" VerticalAlignment="Center" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1">
                                    <FontIcon Glyph="{x:Bind IconGlyph}" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource NeonPrimary}" FontSize="16"/>
                                </Border>

                                <!-- Name & Info -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextStrong}" VerticalAlignment="Center"/>
                                        <Border Grid.Column="1" VerticalAlignment="Center" Padding="8,3" CornerRadius="8" Background="{x:Bind StatusBackground, Mode=OneWay}">
                                            <TextBlock Text="{x:Bind Status, Mode=OneWay}" FontSize="9.5" FontWeight="Bold" Foreground="{x:Bind StatusForeground, Mode=OneWay}" CharacterSpacing="50"/>
                                        </Border>
                                    </Grid>
                                    <TextBlock Text="{x:Bind Info}" FontSize="11" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>

                            <!-- Bottom Row: Inline Action Buttons with borders -->
                            <Grid Grid.Row="1" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,1,0,0" Padding="0,12,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Trust/Block Toggle Action -->
                                <Button Grid.Column="0" Click="BlockDevice_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" Padding="8,4" CornerRadius="8">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE8F8;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="{StaticResource AccentCyan}"/>
                                        <TextBlock Text="Block / Trust" FontSize="11.5" Foreground="{StaticResource AccentCyan}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Button>

                                <!-- Rename Action -->
                                <Button Grid.Column="1" Click="RenameDevice_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" Padding="8,4" CornerRadius="8">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="{StaticResource TextBody}"/>
                                        <TextBlock Text="Rename" FontSize="11.5" Foreground="{StaticResource TextBody}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Button>

                                <!-- Remove Action -->
                                <Button Grid.Column="2" Click="DeleteDevice_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Center" Padding="8,4" CornerRadius="8">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="{StaticResource ColorError}"/>
                                        <TextBlock Text="Remove" FontSize="11.5" Foreground="{StaticResource ColorError}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
