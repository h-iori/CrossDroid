<Page
    x:Class="CrossDroid.Windows.Views.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BgMain}">

    <Grid Padding="36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="About CrossDroid" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextStrong}" />
            <TextBlock Text="App information, versioning and system cache tools" FontSize="14" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Info Grid -->
        <Grid Grid.Row="1" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="360" />
            </Grid.ColumnDefinitions>

            <!-- Left: App Identity -->
            <StackPanel Grid.Column="0" Spacing="20">
                <Border CornerRadius="16" Background="{StaticResource BgElevated}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" Padding="24">
                    <StackPanel Spacing="16">
                        <StackPanel Orientation="Horizontal" Spacing="16">
                            <Border Width="64" Height="64" Background="{StaticResource BgSurface}" CornerRadius="12" BorderBrush="{StaticResource NeonPrimary}" BorderThickness="1.5">
                                <FontIcon Glyph="&#xE104;" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource NeonPrimary}" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="CrossDroid Desktop Companion" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextStrong}"/>
                                <TextBlock Text="Version 2.0.1 (Stable Build)" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="CrossDroid is an end-to-end encrypted local network file sharing suite. This desktop application communicates seamlessly with the Android client to transfer files, folders and archives without cloud dependencies." FontSize="14" Foreground="{StaticResource TextBody}" TextWrapping="Wrap"/>
                        
                        <HyperlinkButton Content="Visit Project Website" NavigateUri="https://github.com/h-iori/CrossDroid" Foreground="{StaticResource AccentCyan}"/>
                    </StackPanel>
                </Border>

                <!-- Storage Clean Card -->
                <Border CornerRadius="16" Background="{StaticResource BgElevated}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" Padding="24">
                    <StackPanel Spacing="16">
                        <TextBlock Text="Storage &amp; Temporary Files" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}"/>
                        <TextBlock Text="When transfers are interrupted or canceled, temporary chunk files can remain on disk. Clean these files to reclaim storage space." FontSize="13" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap"/>
                        
                        <Grid ColumnSpacing="12" Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Temporary Transfer Cache" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextBody}"/>
                                <TextBlock Text="Calculated Size: 12.8 MB" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Clean Storage" Click="CleanStorage_Click" Background="{StaticResource BgSurface}" BorderBrush="{StaticResource ColorError}" Foreground="{StaticResource ColorError}" VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Right: License & Credits -->
            <Border Grid.Column="1" CornerRadius="16" Background="{StaticResource BgElevated}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" Padding="20" VerticalAlignment="Top">
                <StackPanel Spacing="12">
                    <TextBlock Text="Specifications" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}"/>
                    
                    <Grid Padding="0,6" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,0,1">
                        <TextBlock Text="Runtime Environment" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                        <TextBlock Text=".NET 10.0 LTS" FontSize="13" Foreground="{StaticResource TextBody}" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid Padding="0,6" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,0,1">
                        <TextBlock Text="User Interface" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                        <TextBlock Text="WinUI 3 (WASDK 2.0.1)" FontSize="13" Foreground="{StaticResource TextBody}" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid Padding="0,6" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,0,1">
                        <TextBlock Text="Security Primatives" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                        <TextBlock Text="AES-256-GCM / ECDH" FontSize="13" Foreground="{StaticResource TextBody}" HorizontalAlignment="Right"/>
                    </Grid>
                    <Grid Padding="0,6">
                        <TextBlock Text="Installation Package" FontSize="13" Foreground="{StaticResource TextSecondary}"/>
                        <TextBlock Text="MSIX Desktop App" FontSize="13" Foreground="{StaticResource TextBody}" HorizontalAlignment="Right"/>
                    </Grid>

                    <TextBlock Text="© 2026 Iori Studios. All rights reserved." FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,16,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
