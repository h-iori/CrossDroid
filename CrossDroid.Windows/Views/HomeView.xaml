<Page
    x:Class="CrossDroid.Windows.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BgMain}">

    <Page.Resources>
        <!-- Storyboard for QR code scanning line laser -->
        <Storyboard x:Key="ScanLineAnim" RepeatBehavior="Forever">
            <DoubleAnimation 
                Storyboard.TargetName="ScanLineTransform"
                Storyboard.TargetProperty="Y"
                From="0" To="135"
                Duration="0:0:2"
                AutoReverse="True" />
        </Storyboard>
    </Page.Resources>

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition FromHorizontalOffset="0" FromVerticalOffset="28"/>
        </TransitionCollection>
    </Page.Transitions>

    <Grid Padding="24" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Section: Visibility Toggle Switch Card -->
        <Border Grid.Row="0" CornerRadius="16" Background="{StaticResource BgElevated}" BorderBrush="{StaticResource NeonPrimaryBorderBrush}" BorderThickness="1.2" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4" VerticalAlignment="Center">
                    <TextBlock Text="Discovery Mode" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextStrong}"/>
                    <TextBlock x:Name="VisibilityStatusText" Text="Visible to nearby devices" FontSize="12" Foreground="{StaticResource ColorSuccess}"/>
                </StackPanel>
                <ToggleSwitch x:Name="VisibilityToggle" Grid.Column="1" IsOn="True" 
                              OnContent="ON" OffContent="OFF" 
                              Header="" MinWidth="0" VerticalAlignment="Center" 
                              Toggled="VisibilityToggle_Toggled" Foreground="{StaticResource TextBody}"/>
            </Grid>
        </Border>

        <!-- Main Display Panel Area -->
        <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            
            <!-- PANEL A: ACTIVE DISCOVERY (Visible when Toggled On) -->
            <StackPanel x:Name="DiscoveryPanel" Spacing="24" Visibility="Visible" HorizontalAlignment="Center">
                
                <!-- QR Code Display Box with Neon Cyan borders and moving laser scanline -->
                <Border CornerRadius="16" Background="{StaticResource BgSurface}" BorderBrush="{StaticResource AccentCyanBorderBrush}" BorderThickness="1.5" Padding="16" Height="170" Width="170" HorizontalAlignment="Center">
                    <Grid>
                        <!-- Cyberpunk cyan-glowing vector QR Code representation -->
                        <Path Data="M0,0 h40 v20 h-20 v20 h-20 Z M100,0 h40 v40 h-20 v-20 h-20 Z M0,100 h20 v20 h20 v20 h-40 Z M20,20 h20 v20 h-20 Z M100,20 h20 v20 h-20 Z M20,100 h20 v20 h-20 Z M50,50 h40 v40 h-40 Z" Fill="{StaticResource AccentCyan}" Stretch="Uniform"/>
                        
                        <!-- Glow Laser Scan line -->
                        <Rectangle x:Name="ScanLine" Height="3" VerticalAlignment="Top" Fill="{StaticResource NeonHighlight}" Opacity="0.85">
                            <Rectangle.RenderTransform>
                                <TranslateTransform x:Name="ScanLineTransform" Y="0" />
                            </Rectangle.RenderTransform>
                        </Rectangle>
                    </Grid>
                </Border>

                <!-- Secure verification PIN -->
                <StackPanel Spacing="8" HorizontalAlignment="Center">
                    <TextBlock Text="SECURE PAIRING PIN" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" CharacterSpacing="80"/>
                    <Border Background="{StaticResource BgElevated}" CornerRadius="12" Padding="16,10" BorderThickness="1.2" BorderBrush="{StaticResource BorderSubtle}" HorizontalAlignment="Center" Width="160">
                        <TextBlock x:Name="PinTextBlock" Text="8429" FontSize="26" FontWeight="Bold" Foreground="{StaticResource NeonPrimary}" HorizontalAlignment="Center" CharacterSpacing="80" />
                    </Border>
                </StackPanel>

                <!-- Regenerate Refresh Button -->
                <Button Click="RefreshButton_Click" HorizontalAlignment="Center" Height="40" CornerRadius="12" Background="{StaticResource BgElevated}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1" Padding="16,0">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="{StaticResource TextBody}"/>
                        <TextBlock Text="Regenerate PIN" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextBody}"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- PANEL B: INACTIVE DISCOVERY (Visible when Toggled Off) -->
            <StackPanel x:Name="HiddenPanel" Spacing="20" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                
                <!-- Muted privacy/signal-off icon in cyber neon error colors -->
                <Border Width="90" Height="90" CornerRadius="45" Background="#1D12121A" BorderBrush="{StaticResource ColorError}" BorderThickness="1.5" HorizontalAlignment="Center">
                    <FontIcon Glyph="&#xE814;" FontFamily="Segoe MDL2 Assets" FontSize="36" Foreground="{StaticResource ColorError}"/>
                </Border>

                <StackPanel Spacing="6" HorizontalAlignment="Center">
                    <TextBlock Text="DISCOVERY DISABLED" FontSize="12" FontWeight="Bold" Foreground="{StaticResource ColorError}" HorizontalAlignment="Center" CharacterSpacing="100"/>
                    <TextBlock Text="You are not visible to nearby devices" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
